// next.config.js

const nextConfig = {
  reactStrictMode: false,
    experimental: {
        // missingSuspenseWithCSRBailout: false,
        // serverActions:true
      },
};

export default nextConfig;
